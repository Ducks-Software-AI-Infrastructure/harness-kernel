# __AGENT_LABEL__

One-file Harness Kernel project created with `@harness-kernel/create`.

The agent behavior lives in `agent.ts`; runtime composition is explicit in
`run.ts`.

```bash
npm install
npm run run -- "hello"
```
